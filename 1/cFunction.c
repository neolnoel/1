#include <stdio.h>

int parseSet(char *str, int argName[], int argValue[], int argCount) {
    printf("%s\n", str);
    return 0;
}

int parseFunc(char *str) {
    printf("%s\n", str);
    return 0;
}
